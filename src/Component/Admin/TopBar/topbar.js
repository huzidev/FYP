import React from 'react'

function topbar() {
  return (
    <div className='text-red-800'>
        <h2> Topbar Create Later </h2>
        <h4> need to add (search bar, button etc)</h4>

        </div>
  )
}

export default topbar